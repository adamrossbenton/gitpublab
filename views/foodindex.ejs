<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>GitPub App</title>
    </head>
    <body>
        <h1>gitPub Food Menu</h1>
        <ul>
            <% for (let i = 0; i < allFood.length; i++) { %>
                <li>
                    <a href="/food/<%=i%>">
                        <%=allFood[i].name[0].toUpperCase()+allFood[i].name.slice(1) %>
                    </a>
                </li>
            <% } %>
        </ul>
    <a href="/">Back to Main</a>
    </body>
    <style type="text/css">
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</html>